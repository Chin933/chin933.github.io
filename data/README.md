# Data

Put small datasets and replication files here (CSV, dta, R scripts, codebooks).

- Keep individual files under ~100 MB (GitHub Pages limit).
- For large datasets, host them on Dataverse / OSF / ICPSR / a separate GitHub
  repo and link to them from `data.html` instead.
